# Forbidden Context

- Do not read or persist credentials, tokens, or private keys as flow artifacts.
- Do not include full raw command output in `flow_run.yaml`.
- Do not merge, deploy, or publish without an explicit approval signal unless the flow policy says merge approval is disabled.
